29.4.25     Dan
    
    --api/db_init.sql--
    fix: исправлены ошибки, корректная инициализация бд
        - Исправлены ошибки в коде
        - таблицы теперь создаются в правильном порядке (учитываюся все связи между таблицами)
        - добавлены доп ограничения для избежания возникновения ошибок при работе сайта
        - добавлена установка кодировки UTF-8, т.к. на англоязычной системе без этого скрипт работает некорректно.

    --api/auth.php--
    hotFix: исправлены условия проверки пароля и логина
    - Теперь условия проверки ввода пароля и логина совпадают с условиями проверки при создании аккаунта. Теперь не будет ситуаций,
    когда можно было зарегистрироваться например с логином в 3 символа и паролем в 6, а при входе в портал выдавало сообщение о
    некорректно-введёных данных (хотя всё было введено верно)

    --api/register.php--
    hotFix: исправлены условия проверки пароля и логина
    - Теперь условия проверки ввода пароля и логина совпадают с условиями проверки при входе в аккаунт. Теперь не будет ситуаций,
    когда можно было зарегистрироваться например с логином в 3 символа и паролем в 6, а при входе в портал выдавало сообщение о
    некорректно-введёных данных (хотя всё было введено верно)


30.4.25     Dan

    --index.php--
    chore: Исправлены недочеты в дизайне сайта
    - Два верхних блока теперь распределены по ширине страницы (теперь нет той пустоты справа).
    - Нижний блок "Команда проекта" переработан и теперь содержимое сцентрировано, описание участников теперь под их аватарками.
    - Мэйн картинка теперь прикольная и не серая.

    --cabinet.php--
    feat: добавлена кнопка перехода к тестам
        - На странице лк на месте кнопки консультаций теперь есть кнопка перехода к тестам
        - Кнопка запроса консультаций перенесена в правую часть

    --tests/results.php--
    feat: добавлена страница после сохранения результатов теста
    

3.5.25      Dan

    --inserts/pro_qualities_in.sql--
    idk: Этот файл заполняет таблицу professional_qualities всеми ПВК из лекций

    --api/update_quality.php--
    fix: Исправлена ошибка после нажатия "сохранить изменения" при редактированиии ПВК

    --expert/index.php--
    feat: Добавлен тумблер приватности
    - Теперь если эксперт будет выключать тумблер, он не будет появляться в списке "Наши эксперты"
    - Если включит, то будет появляться
    - меняет поле isPublic в таблице users для данного эксперта

    --experts.php--
    Fix: Корректный список экспертов
    - Теперь список будет отображать только тех экспертов, который поставили тумблер "Публичный профиль"

    --api/getPublicStat.php--
    Новый скрипт, получает значение поля isPublic в таблице users для конкретного пользователя (эксперта точнее,
    не буду отдельную таблицу экспертов делать, т. к. всё равно может для всех типов юзеров понадобится)

    --api/updatePublicStat.php--
    Новый скрипт, обновляет значение поля isPublic в таблице users для конкретного пользователя (эксперта пока что),
    в зависимости от положения тумблера

    --api/db_init.sql--
    idk: добавлены поля gender и isPublic
        - isPublic нужно для отображения спимка экспертов (файл experts.php)
        - gender нужно для след лаб


5.5.25      Dan

    --auth/register.php--
    feat: Добавлена возможность выбрать пол пользователю при регистрации
        - Теперь есть выпадающий список с выбором пола

    --api/register.php--
    idk: Реализовано сохранение пола в таблицу users

    --edit_profile.php--
    feat: Пользователь теперь также может редактировать свой пол
        - Теперь есть выпадающий список с выбором пола

    --api/update_profile.php--
    idk: Реализовано изменение пола в users на тот который выбрал пользователь

    --cabinet.php--
    feat: Теперь пользователю отображается его пол

    --api/db_init.sql--
    idk: добавлены ограничения к полю gender в users
        - для избежания ошибок